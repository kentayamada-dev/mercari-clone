import { Box } from "native-base";
import React from "react";
import { SellingDetailTemplateProps } from "./types";

export const SellingDetailTemplate: React.VFC<
  SellingDetailTemplateProps
> = ({}) => {
  return <Box flex={1}>hi</Box>;
};
