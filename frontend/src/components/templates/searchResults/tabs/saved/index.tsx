import { Center, Text } from "native-base";
import React from "react";

type Props = {};

export const SavedTab: React.VFC<Props> = React.memo(({}) => {
  return (
    <Center>
      <Text>hi</Text>
    </Center>
  );
});
